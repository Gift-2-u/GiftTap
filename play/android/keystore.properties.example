# Copy to keystore.properties and fill passwords (do NOT commit real passwords).
# Must match the Bubblewrap TWA key that already published fun.gift2u.tap
storeFile=/home/tower/gift2u-twa/android.keystore
storePassword=YOUR_KEYSTORE_PASSWORD
keyAlias=Gift2u
keyPassword=YOUR_KEY_PASSWORD
